# springboot 내장 서버 환경 설정
# bootwar 실행 시에만 적용이 됨.
# war tomcat 실행 할 때는 적용 X


server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12     # src/main/resources에 p12 파일 복사
    key-store-password: ENC(AZD/oOYNtCPlDC68dabOeA==)
    key-store-type: PKCS12  # 인증서 형식
  servlet:
    encoding:
      charset: UTF-8
      force: true
spring:
  mvc:
    view:
      prefix: /views/
      suffix: .jsp
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shopdb?serverTimezone=Asia/Seoul
    username: smuser
    password: ENC(AZD/oOYNtCPlDC68dabOeA==)
mybatis:
  type-aliases-package: edu.sm.app.dto
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    root: error

# Application Config
app:
  key:
    skey: ${MY_KEY}